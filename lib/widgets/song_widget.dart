// ignore_for_file: prefer_const_constructors
import 'package:flutter/material.dart';
import 'package:flutter_application_1/music/songs.dart';

class SongWidget extends StatelessWidget {
  final Lists lists;

  const SongWidget({Key? key, required this.lists}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ListTile();
  }
}
